{
  "name": "{{NAME}}",
  "version": "{{VERSION}}",
  "type": "module",
  "description": "{{DESCRIPTION}}",
  "license": "MIT",
  "main": "dist/server.js",
  "scripts": {
    "build": "npx tsc --noEmit && cross-env INPUT=mcp-app.html npx vite build && npx tsc -p tsconfig.server.json && bun build server.ts --outdir dist --target node && bun build main.ts --outfile dist/index.js --target node --external \"./server.js\" --banner \"#!/usr/bin/env node\""
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.0.0",
    "@modelcontextprotocol/sdk": "^1.24.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.0",
    "@types/node": "22.10.0",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "typescript": "^5.9.3",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.3.0"
  },
  "bin": {
    "{{NAME}}": "dist/index.js"
  }
}
