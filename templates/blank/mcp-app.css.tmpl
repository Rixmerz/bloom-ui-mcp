.container {
  text-align: center;
  max-width: 400px;
  padding: var(--spacing-lg);
}

.container h1 {
  margin-bottom: var(--spacing-sm);
}

.container p {
  margin-bottom: var(--spacing-lg);
  opacity: 0.8;
}
